Name:- Aditya Mengar
FY CS
ROLL NO. - 33
Div - B

Description:- 
In todays class(28/02/26) i came to know about what actually machine learning is it is about the algorithms it uses for big datasets 
Example you gave was of DOG and CAT how the model actually differentiates btw them by their facial characters like the data which is fed to that model 

and also it uses 
Superwised Learning where the data is fed to the machine such that they work and predict it Accordingly it uses labeled datasets 
Algorithms related to superwised were Linear Regression; Random Forest and 

In Unsuperwised Learning it analyes unlabelled datasets also it detects anomalies and then we came across Semi-Supervised Learnng where model is trained with both labelled and unlabelled data

Also Machine Learning Uses Reinforcement Learning which uses trial and error method and when it gets pass in that work it sees which one is more efficient and fast so that They can reward themselves for the best answer which is faster

and also we came about the treestructure i.e ig Random Forest Structure where we can in subsets we can install different versions like Python 3.11 ; Python 3.8 ; Python 3.14  where in every dataset we can see use it according by installing thatspecific version 
It is of 2 type :
Classification;
Regression ;
In classification for eg we can detect frauds & In final output of the task given the maximum vote is counted which is given to that specific answers
In Regression we can use it to predict stock market where the final output is we can say Average of that Inputs Given 

And Then we discussed about the articles where i came to know about SLM(Small language model) and LLM(Large ......) where SLM is more effiecient and faster and can also work on-device or offline which saves water as LLM database centers need approx. 4 Litres of water for one prompt But LLM are slower than SLM and SLM is fed with domain specific data  (Where You have been currently using Microsoft Phi-3 Model